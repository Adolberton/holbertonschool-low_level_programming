#include "main.h"

/**
 * print_alphabet_x10 - Prints alphabet 10 times
 * print_alphabet_x10: Prototype
 * Return: Always 0 (Success)
 */

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	return (1);
	else
	return (0);
}

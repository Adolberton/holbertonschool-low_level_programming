#include <stdio.h>
/**
 * main - Entry point
 * Description: Write a program that prints _putchar
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;
	for (i = 97; i < 123; i++)
	putchar (i);
	return (0);
}

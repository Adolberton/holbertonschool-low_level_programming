#include <stdio.h>
#include "main.h"

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int main(void)
{
i		print_alphabet();
			return (0);
}
int main(void)
{
	int i;
	for (i = 97; i < 123; i++)
	putchar (i);
	return (0);
}

#include "main.h"

/**
 * print_array - Prints a string in reverse
 * @a: The string to print
 * @n: sqdjfq
 * Return: void
 */
void print_array(int *a, int n)
{
	n = 0;
	while a[n] != '\0'
	{
		printf("%d", a[n]);
		n++;
	}
}

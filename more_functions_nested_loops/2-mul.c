#include "main.h"

/**
* mul - returns 1 if character is lowercase else 0
* @b: The character to be checked.
* @a: encor une desciption useless
* Return: 1 if success, else 0.
*/

int mul(int a, int b)
{	int c = a * b;

	return (c);
}

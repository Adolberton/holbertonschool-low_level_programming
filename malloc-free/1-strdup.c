#include <stdio.h>
#include <stdlib.h>

/**
 * _strdup - Entry point
 * @str : mabit
 * Return: a pointer to the array, or NULL if it fails
 */
char *_strdup(char *str)
{
	char *cp = NULL;

	if (str = NULL)
	{
	return (NULL);
	}
	cp = malloc(sizeof(str))
		
}

